let firstName;
let lastName;

//firstName = 'Daniel'

// || checkea si el valor es null o undefined
console.log(`${firstName || 'No firstName'} ${lastName || 'No LastName'}` )


const isActive = true

const mssg = (isActive) ? 'Activo' : 'Inactivo'

console.log(mssg)